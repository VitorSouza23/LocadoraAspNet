version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - 5000:5000
      - 5001:5001
    depends_on:
      - db
  db:
    image: "mcr.microsoft.com/mssql/server"
    environment: 
      SA_PASSWORD: "P@ssw0rd"
      ACCEPT_EULA: "Y"